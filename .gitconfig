[user]
    email = angeline.muletmarquis@smarkets.com
    name = Angeline Mulet-Marquis

[alias]
    st = status
    co = checkout

[log]
    date = iso

[color]
    diff = auto
    status = auto
    branch = auto
    interactive = auto
    ui = true
    pager = true

[branch]
    autosetuprebase = always

[core]
    editor = vim
    excludesfile = ~/.gitignore_global

[format]
    pretty = %Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr) %C(bold blue)<%an>%Creset

[push]
    default = upstream

[grep]
    lineNumber = true
